# MiniCloudS – Docker (Optional)

Docker support is optional and does not change how MiniCloudS works.
The standard installer is always required.

---

## Requirements
- Docker
- Docker Compose (v2)

---

## Quick Start

From the repository root:

```bash
docker compose up -d
```

## Open in your browser:

```text
http://localhost:18888/install.php
```